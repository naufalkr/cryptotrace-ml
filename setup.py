"""Setup configuration for elisp-cryptotrace-ml package."""
from setuptools import setup, find_packages

setup(
    name="elisp-cryptotrace-ml",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.11",
)
